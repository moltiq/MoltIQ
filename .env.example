# MoltIQ – copy to .env and set as needed (do not commit .env)

# Server (default port 37777)
# MOLTIQ_PORT=37777

# Database (Prisma) – required for server and db:migrate
# DATABASE_URL="file:./data/moltiq.db"

# Chroma vector store (default localhost:8000)
# CHROMA_HOST=localhost
# CHROMA_PORT=8000

# Optional: prune memories older than N days (never prunes pinned/favorite)
# PRUNE_DAYS=90

# Optional: OpenAI embeddings (if unset, stub embeddings are used)
# OPENAI_API_KEY=sk-...

# MCP server base URL (when running MCP tools against HTTP API)
# MOLTIQ_URL=http://localhost:37777
